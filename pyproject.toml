[project]
name = "task-router"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = ["pyyaml>=6.0"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
