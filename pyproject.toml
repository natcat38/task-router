[project]
name = "task-router"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = ["pyyaml>=6.0", "scikit-learn>=1.4", "joblib>=1.3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/task_router"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
