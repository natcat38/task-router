import { Placeholder } from "../components/StatusStates";

/**
 * Runs list (Product_Scope §2 surface 1). Data wiring against
 * GET /runs lands in the next chunk -- this scaffold only proves the
 * route, layout slot, and shell integration.
 */
export function RunsListPage() {
  return <Placeholder title="Runs" />;
}
