import { Placeholder } from "../components/StatusStates";

/**
 * Savings / honesty stats (Product_Scope §2 surface 4, §4 honesty labels).
 * Data wiring against GET /v1/stats lands in a later chunk.
 */
export function StatsPage() {
  return <Placeholder title="Stats" />;
}
